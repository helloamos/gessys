<%= form_with(model: product, local: false) do |form| %>
 <div class="c-modal__body">
     <ul class="errors">
      </ul>

    <div class="row">
      <div class="col-md-6">
         <div class="c-field u-mb-small">
          <%= form.label :name , class: "c-field__label" %>
          <%= form.text_field :name, id: :product_name, class: "c-input", placeholder: "Nom", required: true %>
        </div>
      </div>

       <div class="col-md-6">
        <div class="c-field u-mb-small">
          <%= form.label :product_category_id , class: "c-field__label" %>
          <%= form.collection_select(:product_category_id, @product_categories, :id, :name, {prompt: "Sélectionner une catégorie"}, class: "c-input", required: true) %> 
        </div>
      </div>
    </div>
 
    <div class="row">
      <div class="col-md-6">
         <div class="c-field u-mb-small">
          <%= form.label :unity_id , class: "c-field__label" %>
          <%= form.collection_select(:unity_id, @unities, :id, :name, {prompt: "Sélectionner une unité"}, class: "c-input", required: true) %> 

        </div>

      </div>

       <div class="col-md-6">
          <div class="c-field u-mb-small">
            <%= form.label :unit_price , class: "c-field__label" %>
            <%= form.text_field :unit_price, id: :product_unit_price , class: "c-input", placeholder: "Adresse" %>
        </div>
      </div>

    </div>
   
 

  <div class="c-field u-mb-small">
    <%= form.label :deposit_id, class: "c-field__label" %>
    <%= form.collection_select(:deposit_id, @deposits, :id, :name, {prompt: "Sélectionner un magasin"}, class: "c-input", required: true) %> 

</div>


  <div class="c-field u-mb-small">
    <%= form.label :reorder_threshold, class: "c-field__label"  %>
    <%= form.number_field :reorder_threshold, id: :product_reorder_threshold, class: "c-input", placeholder: "Adresse" %>
  </div>

  </div><!-- modal body -->
            <div class="c-modal__footer">
                    <%= link_to "Annuler", "#", class: "btn", data: {dismiss: "modal"} %>
                    <%= form.submit "Enregistrer", class: "c-btn c-btn--success",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
            </div>
<% end %>
