<%= form_with(model: movement_type, local: true) do |form| %>
  <% if movement_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movement_type.errors.count, "error") %> prohibited this movement_type from being saved:</h2>

      <ul>
      <% movement_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :movement_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :movement_type_description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :movement_type_status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :movement_type_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
