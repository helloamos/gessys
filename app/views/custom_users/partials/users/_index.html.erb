<div class="headers">
                        <div class="left-content">
                        </div>

                        <div class="right-content">
                        	<%= link_to raw('<i class="fa fa-plus" aria-hidden="true"></i> Ajouter'), new_user_path, class: "c-btn c-btn--info", remote: true %>

                        </div>

                    </div>

<div class="ddd">
<table class="c-table" id="datatable">
    <caption class="c-table__title">
        Utilisateurs <small>Liste des utilisateurs</small>
    </caption>
    <thead class="c-table__head c-table__head--slim">
        <tr class="c-table__row">
            <th class="c-table__cell c-table__cell--head">Email</th>
            <th class="c-table__cell c-table__cell--head no-sort">Actions</th>
        </tr>
    </thead>
    <tbody>
        
        
        <% @users.each do |user| %>
        
            <tr class="c-table__row">
                <td class="c-table__cell"><%= user.email %></td>
                <td>
					<div class="action-buttons">
					<%= link_to raw('<i class="fa fa-pencil" aria-hidden="true"></i>
'), edit_user_path(user), title: "Modifier", remote: true %>




<%= link_to raw('<i class="fa fa-trash-o" aria-hidden="true"></i>
'), delete_user_path(user), remote: true, title: "Supprimer", class: "delete" %>
</div>
</td>
            </tr>
        <% end %>
    </tbody>
</table>
</div>